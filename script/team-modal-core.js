// Team Modal Core
class PokemonTeamModalCore {
  constructor(){ this.currentTeam=[]; this.currentIndex=0; }
  init(){ this.createTeamModal(); this.attachEventListeners(); this.addTeamViewButton(); }
  createTeamModal(){ const html=`<div id="pokemonTeamModal" class="modal fade" tabindex="-1" aria-labelledby="pokemonTeamModalLabel" aria-describedby="teamModalContent"><div class="modal-dialog modal-xl"><div class="modal-content"><div class="modal-header"><h5 class="modal-title" id="pokemonTeamModalLabel">🎯 Mein Pokemon-Team</h5><button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Schließen"></button></div><div class="modal-body"><div id="teamModalContent"><div class="placeholder">Team wird geladen...</div></div></div><div class="modal-footer"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Schließen</button><button type="button" class="btn btn-primary" id="analyzeTeamBtn">Team analysieren</button><button type="button" class="btn btn-success" id="exportTeamBtn">Team exportieren</button></div></div></div></div>`; document.body.insertAdjacentHTML('beforeend',html); }
  openTeamModal(){ this.currentTeam=this.getCurrentTeam(); if(!this.currentTeam.length){ alert('Dein Team ist leer!'); return; } this.renderTeamModal(); const modal=document.getElementById('pokemonTeamModal'); if(!modal){ console.error('Pokemon Team Modal not found'); return; }
    // Offene andere Modals schließen & Fokus säubern um aria-hidden Konflikte zu vermeiden
    document.querySelectorAll('.modal.show').forEach(mo=>{ if(mo!==modal){ if(mo.contains(document.activeElement)) document.activeElement.blur(); const inst=bootstrap.Modal.getInstance(mo); if(inst) inst.hide(); }});
    let m=bootstrap.Modal.getInstance(modal); if(!m) m=new bootstrap.Modal(modal,{backdrop:true,keyboard:true,focus:true}); m.show(); }
  getCurrentTeam(){ const dp=document.querySelector('.drop-point'); if(!dp) return []; return Array.from(dp.querySelectorAll('.pokemon-card')).map(c=> this.extractPokemonData(c)).filter(Boolean); }
  extractPokemonData(card){ const id=parseInt(card.dataset.pokemonId||card.dataset.id); const name=card.querySelector('.pokemon-name')?.textContent||''; const number=card.querySelector('.pokemon-number')?.textContent||''; const image=card.querySelector('.pokemon-image')?.src||''; const types=Array.from(card.querySelectorAll('.type-badge, .pokemon-types span')).map(el=> el.textContent.toLowerCase().replace(/[^a-z]/g,'')).filter(t=>t); const fav=window.pokemonGoFeatures?.isFavorite(id)||false; const rating=window.pokemonGoFeatures?.getRating(id)||0; const note=window.pokemonGoFeatures?.getNote(id)||''; if(id&&name&&types.length) return {id,name,number,image,types,isFavorite:fav,rating,note}; return null; }
}
window.PokemonTeamModalCore=PokemonTeamModalCore;